//compare two strings
#include<stdio.h>
int main(){
    char str[11]="hello";
    char str2[11]="hello";

    int i;
     if(str[i]==str2[i])
       {
           printf("strings are equal\n");
       }
       else
       {
           printf("strings are not equal\n");
       }

    
    return 0;
}