#include<stdio.h>
int main(){
    int n,i,dig;
    printf("enter the number");
    scanf("%d",&n);
    for(i=0;i<=n;i++)
    {
        dig=n%10;
    }
    printf("last digit digit is %d",dig);
return 0;    
    
}